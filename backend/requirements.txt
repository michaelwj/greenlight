# Runtime dependencies — mirrors [project.dependencies] in pyproject.toml.
# For host-side script runs: python -m venv .venv && .venv/bin/pip install -r requirements.txt
# (scripts also run inside the container: docker compose exec api python scripts/admin_cli.py ...)
alembic>=1.14.0
anthropic>=0.116.0
asyncpg>=0.30.0
fastapi>=0.116.0
httpx>=0.28.1
pydantic>=2.10.0
pydantic-settings>=2.8.0
python-dotenv>=1.0.1
pywebpush>=2.0.0
redis>=5.2.0
rq>=1.16.2
sqlalchemy>=2.0.36
uvicorn[standard]>=0.34.0
yt-dlp[default,curl-cffi]>=2025.6.30
